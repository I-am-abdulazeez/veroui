{
  "name": "veroui",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "story:dev": "pnpm --filter @veroui/storybook dev",
    "story:build": "pnpm --filter @veroui/storybook build",
    "story:preview": "pnpm --filter @veroui/storybook preview"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
